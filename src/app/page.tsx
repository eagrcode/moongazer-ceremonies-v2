import Hero from "@/components/hero";

export default function Home() {
  return (
    <main className="relative flex w-screen flex-col items-center overflow-hidden">
      <Hero />
    </main>
  );
}
